databases:
  - name: bassly-db
    plan: free

services:
  - type: web
    name: bassly
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bassly-db
          property: connectionString
